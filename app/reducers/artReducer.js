const initialArtState = {
  art: {
    name: "",
    creator: "",
    description: "",
    image: ""
  }
}

const artReducer = (state=initialArtState, action) => {
  return state
}

export default artReducer